spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/testdb
    username: test
    password: test
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  
  flyway:
    enabled: false

clerk:
  jwks-uri: https://test.clerk.example.com/.well-known/jwks.json
  issuer: https://test.clerk.example.com

cors:
  allowed-origins: http://localhost:3000

logging:
  level:
    io.pipeline: WARN
    org.springframework: WARN
