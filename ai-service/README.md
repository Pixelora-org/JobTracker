# AI Service (Future)

**Status**: Reserved for Phase 4 service split

This directory is currently a placeholder. AI functionality is stubbed in the `backend/` directory under the `io.pipeline.ai` package.

## Future Purpose

When the monolithic backend is split into microservices (Phase 4), this directory will contain:

- AI/LLM integration service
- Resume parsing
- Job matching algorithms
- Outreach message suggestions
- Embeddings and vector search

## Current Implementation

For now, see `backend/src/main/java/io/pipeline/ai/` (currently just a package stub).

AI features are planned for Phase 3+ of the migration.

Refer to [`docs/architecture.md`](../docs/architecture.md) for the full migration plan.
